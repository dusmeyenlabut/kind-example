apiVersion: v1
kind: Service
metadata:
  name: external-ui-service
spec:
  selector:
    k8s-app: dockerhost
  clusterIP: None
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ui-ingress
  annotations:
    kubernetes.io/ingress.class: "traefik-i3-access"
spec:
  rules:
    - host: st1.mo360mes.localhost
      http:
        paths:
          - pathType: Prefix
            path: /ui
            backend:
              service:
                name: external-ui-service
                port:
                  number: 80