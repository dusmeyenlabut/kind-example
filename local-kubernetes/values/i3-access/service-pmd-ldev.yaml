apiVersion: v1
kind: Service
metadata:
  name: external-pmd-service
spec:
  selector:
    k8s-app: dockerhost
  clusterIP: None
  ports:
    - port: 80
      targetPort: 9003
      protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pmd-ingress
  annotations:
    kubernetes.io/ingress.class: "traefik-i3-access"
spec:
  rules:
    - host: st1.mo360mes.localhost
      http:
        paths:
          - pathType: Prefix
            path: /productmasterdata
            backend:
              service:
                name: external-pmd-service
                port:
                  number: 80